# Vending-machine-controller
A **Vending Machine Controller** is a sequential digital circuit that manages product selection, coin input, and change calculation. It automatically dispenses the selected product when the inserted amount is sufficient.
